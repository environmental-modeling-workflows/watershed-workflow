.. _hydrology:

Hydrology
=========

The :mod:`watershed_workflow.hydro` subpackage provides the
:class:`~watershed_workflow.hydro.river.River` and
:class:`~watershed_workflow.hydro.watershed.Watershed` classes, along
with functions for manipulating and cleaning up river networks and
watershed boundaries.

.. automodule:: watershed_workflow.hydro
   :members:
   :imported-members:
