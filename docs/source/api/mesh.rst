.. _mesh:

Mesh
====

The :mod:`watershed_workflow.mesh` subpackage provides mesh data
structures (:class:`~watershed_workflow.mesh.mesh.Mesh2D`,
:class:`~watershed_workflow.mesh.mesh.Mesh3D`) and the triangulation,
conditioning, and region-labeling functions used to build them.

.. automodule:: watershed_workflow.mesh
   :members:
   :imported-members:
